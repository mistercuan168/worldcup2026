# Copy to ".streamlit/secrets.toml" for LOCAL use, OR paste these into the
# Streamlit Community Cloud "Secrets" box when deploying. Never commit the real file.
# Both keys are optional — the app works on the committed historical CSV alone.

FOOTBALL_DATA_API_KEY = ""
API_FOOTBALL_KEY = ""
